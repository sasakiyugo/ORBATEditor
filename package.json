{
  "name": "orbat-editor",
  "version": "0.8.1",
  "private": true,
  "description": "Offline ORBAT editor with NATO-style symbols and automatic roll-ups",
  "scripts": {
    "test": "node --check app.js && node --check map-data-01.js && node --check map-data-20.js && node tests/smoke.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
